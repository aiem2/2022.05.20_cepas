<div id="<%= dom_id wine %>">
  <p>
    <strong>Name:</strong>
    <%= wine.name %>
  </p>

  <p>
    <strong>Score:</strong>
    <%= wine.score %>
  </p>

  <% wine.wine_strains.each do |wine_spec|%>
    <li><%= wine_spec.strain.name %> (<%= wine_spec.percentage %> %)</li>
  <% end%>
  

</div>
