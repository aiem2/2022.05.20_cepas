<div class="nested-fields">
    <div>
        <%= f.label :strain %>
        <%= f.collection_select :strain_id, @strains, :id, :name, :prompt => "Selecciones una opción:" %>
    </div>

    <div>
        <%= f.label :percentage %>
        <%= f.number_field :percentage %>
    </div>

    <div>
        <%= link_to_remove_association 'Remover Cepa', f %>
    </div>
</div>
