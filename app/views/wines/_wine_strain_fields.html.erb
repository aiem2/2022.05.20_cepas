<div class="nested-fields">
    <div>
        <%= f.label :strain %>
        <%= f.collection_select :strain_id, @strains, :id, :name, :prompt => "Choose an option:" %>
    </div>

    <div>
        <%= f.label :percentage %>
        <%= f.number_field :percentage %>
    </div>

    <div>
        <%= link_to_remove_association 'Delete strain', f %>
    </div>
</div>
