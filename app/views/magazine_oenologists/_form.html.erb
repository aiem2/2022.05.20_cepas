<%= form_with(model: magazine_oenologist) do |form| %>
  <% if magazine_oenologist.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(magazine_oenologist.errors.count, "error") %> prohibited this magazine_oenologist from being saved:</h2>

      <ul>
        <% magazine_oenologist.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  
  <div>
    <%= form.label :magazine_id, style: "display: block" %>
    <%= form.number_field :magazine_id %>
  </div>

  <div>
    <%= form.label :oenologist_id, style: "display: block" %>
    <%= form.number_field :oenologist_id %>
  </div>

  <div>
    <%= form.select :area, @jobs, :prompt => "Selecciones una opción:", class:"form-control"%>
  </div>
  
  <div>
    <%= form.submit %>
  </div>
<% end %>
